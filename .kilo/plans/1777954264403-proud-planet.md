# Plan: Update AI Chat Project to Clinic Management System

## Overview
Change the "AI Chat Application" project (index 2) to "Clinic Management System" with updated translations and image.

## Changes Required

### 1. Update English messages (`messages/en.json`)
- Line 101-104: Change project title from "AI Chat Application" to "Clinic Management System"
- Update description to mention: "A comprehensive clinic management system built with React, TypeScript, Tailwind CSS, and PostgreSQL for patient records, appointments, and billing."
- Update tags to: ["React", "TypeScript", "Tailwind CSS", "PostgreSQL"]

### 2. Update Russian messages (`messages/ru.json`)
- Line 101-104: Change project title from "AI Chat-приложение" to "Система управления клиникой"
- Update description to mention: "Комплексная система управления клиникой на React, TypeScript, Tailwind CSS и PostgreSQL для записей пациентов, назначений и выставления счетов."
- Update tags accordingly

### 3. Update Uzbek messages (`messages/uz.json`)
- Line 101-104: Change project title from "AI Chat Ilovasi" to "Klinika Boshqaruvi Tizimi"
- Update description in Uzbek mentioning the tech stack
- Update tags accordingly

### 4. Add new image file
- Add `/public/clinic.png` image file (user needs to provide this image)
- OR use existing image if clinic.png already exists

### 5. No code changes needed in `components/main/Projects.tsx`
- The component already uses the correct image mapping (index 2 = "/NextWebsite.png")
- After adding clinic.png, update the `imageByIndex` array in Projects.tsx:
  - Change line 28 from "/NextWebsite.png" to "/clinic.png"

## Implementation Order
1. First add the clinic.png image to the public folder
2. Update the Projects.tsx to use clinic.png instead of NextWebsite.png
3. Update all three translation files (en.json, ru.json, uz.json)

## Files to Modify
- `messages/en.json`
- `messages/ru.json`
- `messages/uz.json`
- `components/main/Projects.tsx` (image path change)